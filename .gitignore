# Local .terraform directories
**/.terraform/*

# Terraform lockfile (commit this if you want reproducible provider versions)
# .terraform.lock.hcl

# Terraform state files - NEVER commit these (contain sensitive data)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform plan output files
*.tfplan
*.tfplan.*

# Crash log files
crash.log
crash.*.log

# Terraform variable files that may contain secrets
*.tfvars
*.tfvars.json

# Override files (used for local overrides, not for sharing)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI configuration file
.terraformrc
terraform.rc

# Sensitive files
*.pem
*.key
*.p12
*.pfx

# macOS
.DS_Store

# Editor directories and files
.idea/
.vscode/
*.swp
*.swo
